server:
  port: 8080
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:file:./.h2/testdb
    data-username: test
    password: test
    driver-class-name: org.h2.Driver
